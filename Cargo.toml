[package]
name = "gorka"
version = "0.1.0"
edition = "2021"
description = "Compression for GLONASS/GNSS telemetry (FDMA-aware)"
license = "MIT OR Apache-2.0"
keywords = ["gnss", "glonass", "compression", "telemetry", "tsdb"]
categories = ["compression", "encoding", "decoding"]

[lib]
name = "gorka"
path = "src/lib.rs"

[dependencies]

[dev-dependencies]
proptest = "1.11.0"
criterion = "0.8.2"
